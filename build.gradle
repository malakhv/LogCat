// Auto-generated code
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies { classpath 'com.android.tools.build:gradle:4.2.1' }
}
allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url "file://${rootDir}/artifacts/logcat" }
        maven { url "file://${rootDir}/../zMaven" }
    }
}
task clean(type: Delete) { delete rootProject.buildDir }