// Auto-generated code
buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies { classpath 'com.android.tools.build:gradle:3.6.4' }
}
allprojects {
    repositories {
        google()
        jcenter()
        maven { url "file://${rootDir}/artifacts/logcat" }
        maven { url "file://${rootDir}/../zMaven" }
    }
}
task clean(type: Delete) { delete rootProject.buildDir }